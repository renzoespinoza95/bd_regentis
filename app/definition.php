<?php
  global $varhost;
  global $apphost;
  global $app;
  
  global $ssa_id;  
  global $mustache;  
  global $nombre_app; 
  global $administrador_actual;
    
  global $apikey_openai;
  global $apiKey;
  
  global $wkh_pdf;
  global $host;
  global $username;
  global $password;
  global $dbname;
  global $secreto_jwt;
  global $version;

  