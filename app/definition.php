<?php
  global $varhost;
  global $apphost;
  global $apihost;
  
  global $app;
  
  global $sesion_admin_administrador_id;  
  global $mustache;  
  global $nombre_app; 
  
  
  global $administrador;
  global $upload;
  global $mimetype;
  global $simple_image;
  global $objPHPExcel;
  
  global $administrador_actual;
  global $info_admin; 
  
  
  global $path_public;
  global $path_data;
  global $apikey_openai;
  global $apiKey;
  
  global $varhost_html;
  global $varhost_admin;
  global $varhost_data;
  global $varhost_data_web;
  global $varhost_public;
  global $bootstrap;
  global $wkh_pdf;
  global $ruta_log;
  global $host;
  global $username;
  global $password;
  global $dbname;
  global $secreto_jwt;

  global $file_log;